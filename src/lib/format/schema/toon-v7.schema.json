{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:toon-platform:format:toon-v7",
  "title": "Toon animation document, schema version 7",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "width",
    "height",
    "frame_rate",
    "tools",
    "layers"
  ],
  "properties": {
    "schema_version": {
      "const": 7
    },
    "width": {
      "type": "integer",
      "minimum": 1,
      "maximum": 32767
    },
    "height": {
      "type": "integer",
      "minimum": 1,
      "maximum": 32767
    },
    "frame_rate": {
      "type": "integer",
      "minimum": 1,
      "maximum": 60
    },
    "tools": {
      "type": "array",
      "maxItems": 65536,
      "items": {
        "$ref": "#/$defs/tool"
      }
    },
    "layers": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "$ref": "#/$defs/layer"
      }
    }
  },
  "$defs": {
    "layer": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "hidden",
        "frames"
      ],
      "properties": {
        "hidden": {
          "type": "boolean"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 12
        },
        "frames": {
          "type": "array",
          "minItems": 1,
          "maxItems": 4096,
          "items": {
            "$ref": "#/$defs/frame"
          }
        }
      }
    },
    "tool": {
      "oneOf": [
        {
          "$ref": "#/$defs/pencil"
        },
        {
          "$ref": "#/$defs/eraser"
        },
        {
          "$ref": "#/$defs/contour"
        },
        {
          "$ref": "#/$defs/contour-eraser"
        },
        {
          "$ref": "#/$defs/feather"
        },
        {
          "$ref": "#/$defs/stamp"
        }
      ]
    },
    "pencil": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "geometry",
        "width",
        "color"
      ],
      "properties": {
        "kind": {
          "const": "pencil"
        },
        "geometry": {
          "description": "How the stroke's numbers are read into a path.",
          "enum": [
            "line",
            "smooth",
            "cubic"
          ]
        },
        "width": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4800
        },
        "color": {
          "type": "string",
          "pattern": "^#[0-9a-f]{6}$"
        }
      }
    },
    "eraser": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "geometry",
        "width"
      ],
      "properties": {
        "kind": {
          "const": "eraser"
        },
        "geometry": {
          "description": "How the stroke's numbers are read into a path.",
          "enum": [
            "line",
            "smooth",
            "cubic"
          ]
        },
        "width": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4800
        }
      }
    },
    "contour": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "geometry",
        "color"
      ],
      "properties": {
        "kind": {
          "const": "contour"
        },
        "geometry": {
          "description": "How the stroke's numbers are read into a path.",
          "enum": [
            "line",
            "smooth",
            "cubic"
          ]
        },
        "color": {
          "type": "string",
          "pattern": "^#[0-9a-f]{6}$"
        }
      }
    },
    "contour-eraser": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "geometry"
      ],
      "properties": {
        "kind": {
          "const": "contour-eraser"
        },
        "geometry": {
          "description": "How the stroke's numbers are read into a path.",
          "enum": [
            "line",
            "smooth",
            "cubic"
          ]
        }
      }
    },
    "frame": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "strokes"
      ],
      "properties": {
        "strokes": {
          "type": "array",
          "maxItems": 16384,
          "items": {
            "$ref": "#/$defs/stroke"
          }
        }
      }
    },
    "stroke": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "points",
        "tool_id"
      ],
      "properties": {
        "points": {
          "type": "array",
          "minItems": 2,
          "maxItems": 65536,
          "items": {
            "type": "integer",
            "minimum": -32768,
            "maximum": 32767
          }
        },
        "tool_id": {
          "type": "integer",
          "minimum": 0,
          "maximum": 65535
        },
        "pressure": {
          "description": "Pen pressure per point, 0-100; one value per x,y pair. Absent means constant width.",
          "type": "array",
          "minItems": 1,
          "maxItems": 32768,
          "items": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          }
        }
      }
    },
    "feather": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "geometry",
        "width",
        "color",
        "fill"
      ],
      "properties": {
        "kind": {
          "const": "feather"
        },
        "geometry": {
          "description": "How the stroke's numbers are read into a path.",
          "enum": [
            "line",
            "smooth",
            "cubic"
          ]
        },
        "width": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4800
        },
        "color": {
          "type": "string",
          "pattern": "^#[0-9a-f]{6}$"
        },
        "fill": {
          "type": "string",
          "pattern": "^#[0-9a-f]{6}$"
        }
      }
    },
    "stamp": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "geometry",
        "width",
        "color",
        "shape"
      ],
      "properties": {
        "kind": {
          "const": "stamp"
        },
        "geometry": {
          "description": "How the stroke's numbers are read into a path.",
          "enum": [
            "line"
          ]
        },
        "width": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4800
        },
        "color": {
          "type": "string",
          "pattern": "^#[0-9a-f]{6}$"
        },
        "shape": {
          "description": "Closed polygon on the unit square, filled at every point of the stroke: [x0,y0,x1,y1,...], at least three points.",
          "type": "array",
          "items": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          },
          "minItems": 6,
          "maxItems": 128
        }
      }
    }
  }
}
